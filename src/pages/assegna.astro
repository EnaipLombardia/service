---
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabaseClient';

const urlParams = new URL(Astro.request.url).searchParams;
const assetId = urlParams.get('id');
let asset = null;
let currentAssignment = null;

if (assetId) {
  const { data } = await supabase
    .from('asset')
    .select('*')
    .eq('id', assetId)
    .single();
  asset = data;

  if (asset) {
    const { data: assData } = await supabase
      .from('assegnazioni')
      .select('*')
      .eq('asset_id', assetId)
      .is('data_fine', null)
      .single();
    currentAssignment = assData;
  }
}
---

<Layout title="Assegna Asset">
  <div class="p-4 max-w-2xl mx-auto">
    <h1 class="text-2xl font-bold text-[#8b5a2b] dark:text-[#c49a6c] mb-4">🔄 Assegna Asset</h1>
    
    {asset ? (
      <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-6">
        <div class="mb-4 p-3 bg-gray-100 dark:bg-gray-700 rounded">
          <p class="text-gray-700 dark:text-gray-300"><strong>Asset:</strong> {asset.marca} {asset.modello}</p>
          <p class="text-gray-700 dark:text-gray-300"><strong>Seriale:</strong> {asset.numero_serie}</p>
          <p class="text-gray-700 dark:text-gray-300"><strong>Stato attuale:</strong> {asset.stato}</p>
        </div>

        {currentAssignment ? (
          <div class="mb-4 p-3 bg-yellow-50 dark:bg-yellow-900/30 rounded border border-yellow-200 dark:border-yellow-700">
            <p class="text-yellow-700 dark:text-yellow-400">
              ⚠️ Questo asset è già assegnato a <strong>{currentAssignment.dipendente_nome}</strong>
            </p>
            <p class="text-sm text-gray-600 dark:text-gray-400">Dal {currentAssignment.data_inizio}</p>
            <p class="text-sm mt-2">
              <a href={`/restituisci?id=${asset.id}`} class="text-[#006a4e] dark:text-[#4a9a7e] hover:underline">
                📤 Restituiscilo prima di riassegnarlo
              </a>
            </p>
          </div>
        ) : (
          <form id="assignForm">
            <div class="mb-4">
              <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                Assegna a <span class="text-red-500">*</span>
              </label>
              <input
                type="text"
                id="dipendente"
                class="w-full border border-gray-300 dark:border-gray-600 rounded px-3 py-2 bg-white dark:bg-gray-700 text-gray-900 dark:text-white placeholder-gray-500 dark:placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-[#006a4e] transition-colors"
                placeholder="Nome o email del dipendente"
                required
              />
            </div>

            <div class="mb-4">
              <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                Motivo <span class="text-red-500">*</span>
              </label>
              <select
                id="motivo"
                class="w-full border border-gray-300 dark:border-gray-600 rounded px-3 py-2 bg-white dark:bg-gray-700 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-[#006a4e] transition-colors"
                required
              >
                <option value="Consegna">Consegna</option>
                <option value="Sostituzione">Sostituzione</option>
                <option value="Trasferimento">Trasferimento</option>
                <option value="Altro">Altro</option>
              </select>
            </div>

            <div class="flex gap-2">
              <button
                type="button"
                id="assegnaBtn"
                class="flex-1 bg-[#006a4e] hover:bg-[#005a3e] text-white px-4 py-2 rounded transition-colors"
              >
                ✅ Conferma Assegnazione
              </button>
              <a
                href={`/asset/${asset.id}`}
                class="bg-[#8b5a2b] hover:bg-[#7a4a1b] text-white px-4 py-2 rounded transition-colors"
              >
                Annulla
              </a>
            </div>

            <div id="messaggio" class="mt-4 hidden"></div>
          </form>
        )}

        <div class="mt-4">
          <a href={`/asset/${asset.id}`} class="text-[#006a4e] dark:text-[#4a9a7e] hover:underline">← Torna al dettaglio</a>
        </div>
      </div>
    ) : (
      <div class="bg-yellow-50 dark:bg-yellow-900/30 p-4 rounded">
        <p class="text-yellow-700 dark:text-yellow-400">⚠️ Nessun asset selezionato.</p>
        <a href="/" class="text-[#006a4e] dark:text-[#4a9a7e] hover:underline">← Torna alla dashboard</a>
      </div>
    )}
  </div>

  <script>
    import { supabase } from '../lib/supabaseClient';

    document.getElementById('assegnaBtn')?.addEventListener('click', async function() {
      const messaggio = document.getElementById('messaggio');
      const assetId = new URLSearchParams(window.location.search).get('id');
      const dipendente = document.getElementById('dipendente').value;
      const motivo = document.getElementById('motivo').value;

      if (!dipendente) {
        messaggio.className = 'mt-4 p-3 bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400 rounded';
        messaggio.textContent = '⚠️ Inserisci il nome del dipendente';
        return;
      }

      try {
        await supabase
          .from('assegnazioni')
          .update({ data_fine: new Date().toISOString().split('T')[0] })
          .eq('asset_id', assetId)
          .is('data_fine', null);

        const { error } = await supabase
          .from('assegnazioni')
          .insert([{
            asset_id: assetId,
            dipendente_email: dipendente,
            dipendente_nome: dipendente,
            data_inizio: new Date().toISOString().split('T')[0],
            motivo: motivo
          }]);

        if (error) throw error;

        await supabase
          .from('asset')
          .update({ stato: 'Assegnato' })
          .eq('id', assetId);

        messaggio.className = 'mt-4 p-3 bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-400 rounded';
        messaggio.textContent = `✅ Asset assegnato a ${dipendente} con successo!`;
        
        setTimeout(() => {
          window.location.href = `/asset/${assetId}`;
        }, 2000);

      } catch (error) {
        console.error('Errore:', error);
        messaggio.className = 'mt-4 p-3 bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400 rounded';
        messaggio.textContent = `❌ Errore: ${error.message}`;
      }
    });
  </script>
</Layout>
