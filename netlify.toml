[build]
  command = "astro build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Disabilita Cypress completamente
[[plugins]]
  package = "netlify-plugin-cypress"
  [plugins.inputs]
    enable = false
    # Oppure: enable = false
